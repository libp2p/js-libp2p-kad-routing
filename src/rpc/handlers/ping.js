'use strict'

/**
 * Process `Ping` DHT messages.
 *
 * @param {DHT} dht
 * @param {PeerInfo} peer
 * @param {Message} msg
 * @param {function(Error, Message)} callback
 * @returns {undefined}
 */
function ping (dht, peer, msg, callback) {

}

module.exports = ping

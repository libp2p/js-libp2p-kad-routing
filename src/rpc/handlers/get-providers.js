'use strict'

/**
 * Process `GetProviders` DHT messages.
 *
 * @param {DHT} dht
 * @param {PeerInfo} peer
 * @param {Message} msg
 * @param {function(Error, Message)} callback
 * @returns {undefined}
 */
function getProviders (dht, peer, msg, callback) {

}

module.exports = getProviders

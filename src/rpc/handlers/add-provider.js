'use strict'

/**
 * Process `AddProvider` DHT messages.
 *
 * @param {DHT} dht
 * @param {PeerInfo} peer
 * @param {Message} msg
 * @param {function(Error, Message)} callback
 * @returns {undefined}
 */
function addProvider (dht, peer, msg, callback) {

}

module.exports = addProvider

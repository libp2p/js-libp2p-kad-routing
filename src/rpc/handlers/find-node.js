'use strict'

/**
 * Process `FindNode` DHT messages.
 *
 * @param {DHT} dht
 * @param {PeerInfo} peer
 * @param {Message} msg
 * @param {function(Error, Message)} callback
 * @returns {undefined}
 */
function findNode (dht, peer, msg, callback) {

}

module.exports = findNode

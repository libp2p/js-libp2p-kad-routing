'use strict'

/**
 * Process `PutValue` DHT messages.
 *
 * @param {DHT} dht
 * @param {PeerInfo} peer
 * @param {Message} msg
 * @param {function(Error, Message)} callback
 * @returns {undefined}
 */
function putValue (dht, peer, msg, callback) {

}

module.exports = putValue
